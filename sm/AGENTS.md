# sm — Probability & statistics scenes

- Sample space sets: `\mathcal{A}`, `\mathcal{B}`, ... (use `MathTex(r"\mathcal{A} = \{...\}", ...)`)
- Expected value / mean: `\langle E \rangle` (use `MathTex(r"\langle E \rangle", ...)`)
- Scenes follow root conventions (see `../AGENTS.md`): explicit manim imports, `pXX_descripcion.py` naming, `ruff check` after writing (no manim commands).
